    <section class="officials-section">
        <div class="container">
            <div class="carousel-container">
                <div class="carousel-track row">
                {% for row in rows %}
                    {{ row.content }}
                {% endfor %} 
                </div>
            </div>
        </div>
    </section>