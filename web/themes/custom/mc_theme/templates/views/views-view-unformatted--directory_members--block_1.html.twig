    <section class="officials-section">
        <div class="container">
            <div class="carousel-container">
                <div class="carousel-track slick-slider">
                {% for row in rows %}
                    {{ row.content }}
                {% endfor %} 
                </div>
            </div>
        </div>
    </section>