<section class="info-section" style="background-image: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)),url('{{ file_url(paragraph.field_background_image.entity.field_media_image.entity.uri.value) }}')">
    <div class="container">
        <div class="info-content">
            <h2 class="info-title">{{ content.field_title|raw }}</h2>
            <div class="info-subtitle">{{ content.field_sub_title|raw }}</div>
            {{ content.field_latest_news_view }}
        </div>
    </div>
</section>