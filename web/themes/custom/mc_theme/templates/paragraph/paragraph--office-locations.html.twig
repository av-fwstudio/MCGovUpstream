{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}
        {% set image = file_url(content.field_background_iamge.0['#media'].field_media_image.entity.fileuri) %}
        <div class="office-location-wrapper {% if image|length > 0 %}background-image{% endif %}"{% if image|length > 0 %} style="background-image: linear-gradient(0deg, rgba(0, 0, 0, 0.54) 0%, rgba(0, 0, 0, 0.54) 100%),url('{{ image }}')"{% endif %}>
            <div class="container">
                <div class="row {{ content.field_placement.0['#markup']|clean_class }}">
                    <div class="col-md-4">
                        <div class="office-location-content">
                            <div class="title">{{ content.field_title.0['#text'] }}</div>
                            <div class="sub-title">{{ content.field_sub_title.0['#text'] }}</div>
                            <div class="description">{{ content.field_description.0['#text']|raw }}</div>
                            <div class="office-address">{{ content.field_office_address.0['#text']|raw }}</div>
                            <div class="get-direction-cta"><a href="{{ content.field_get_direction.0['#url'].uri }}">{{ content.field_get_direction.0['#title'] }}</a></div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        {% set map_url = paragraph.field_map_link.0.url.toString %}
                        {% if map_url %}
                            <iframe
                                    src="{{ map_url }}"
                                    width="100%"
                                    height="100%"
                                    style="border:0;"
                                    allowfullscreen=""
                                    loading="lazy"
                                    referrerpolicy="no-referrer-when-downgrade">
                            </iframe>
                        {% endif %}
                    </div>
                    <div class="col-md-4">
                        <div class="contact-details">
                            <div class="contact-card">
                                {% for key, item in content.field_contact_details %}
                                    {% if key|first != '#' %}
                                        {% set paragraph = item['#paragraph'] %}
                                        <div class="contact-section">
                                            {% set media_entity = paragraph.field_contact_detail_icon.entity %}
                                            {% if media_entity %}
                                                {% set image_field = media_entity.field_media_image %}
                                                {% if image_field.entity %}
                                                    {% set image_url = image_field.entity.getFileUri()|file_url %}
                                                    <div class="contact-icon">
                                                        <div class="icon-img"><img src="{{ image_url }}" alt="{{ image_field.alt }}" /></div>
                                                    </div>
                                                {% endif %}
                                            {% endif %}
                                            <div class="contact-details">
                                                {% set type_field = paragraph.field_contact_detail_type %}
                                                {% if type_field %}
                                                    {% for type_entity in type_field %}
                                                        {% if type_entity.entity %}
                                                            {% set value = type_entity.entity.field_value.value %}
                                                            <div class="contact-type">
                                                                <div class="contact-title">{{ type_entity.entity.field_title.value }}</div>
                                                                <div class="contact-value">
                                                                    {% if value matches '/@/' %}
                                                                        <a href="mailto:{{ value }}">{{ value }}</a>
                                                                    {% elseif value|replace({' ': '', '+': '', '-': '', '(': '', ')': ''}) matches '/^[0-9]+$/' %}
                                                                        <a href="tel:{{ value|replace({' ': ''}) }}">{{ value }}</a>
                                                                    {% else %}
                                                                        {{ value }}
                                                                    {% endif %}
                                                                </div>
                                                            </div>
                                                        {% endif %}
                                                    {% endfor %}
                                                {% endif %}
                                            </div>
                                        </div>
                                    {% endif %}
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
  </div>
{% endblock paragraph %}
